


Java加密扩展（JCE）无限强度管理策略文件

curl localhost:10100/omsUser/dev

curl http://root:123456@localhost:10100/encrypt -d 13873884179ok

keytool -genkeypair -alias jacliuKey -keyalg RSA \ -dname "CN=Web Server,OU=Unit,O=Organization,L=City,S=State,C=US" \ -keypass jacliuSecret -keystore server.jks -storepass 123456
  
  
  
  $ keytool -genkeypair -alias mytestkey -keyalg RSA \
  -dname "CN=Web Server,OU=Unit,O=Organization,L=City,S=State,C=US" \
  -keypass changeme -keystore server.jks -storepass letmein

encrypt:
  keyStore:
    location: classpath:/server.jks
    password: letmein
    alias: mytestkey
    secret: changeme